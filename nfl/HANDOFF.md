# HANDOFF

Generated by `nfl/tools/agent_state.py` at 2026-09-18T18:48:35.611521+00:00. Do not hand-edit: regenerate.

## Current objective

Produce NFL player-prop and DFS forecasts that discriminate between games materially better than the current baseline, with honest calibration and a fully auditable research process. Market data may evaluate a forecast and may never feed one. **V2 NOT YET EARNED.**

## Where the repository is

- branch `claude/nfl-greenfield-architecture-stsxmk` at `4bb940d` — P8: Contract 4 was already reconciled; what was missing was the check
- source scope: clean (20 dirty tree entries in total, source and not)

## Active task

**A3** (queue position 5) — Automatic Scientist — test `A3_ROLE_CONTINUITY_ACROSS_REGIME_CHANGES` against measurement.

Acceptance criteria not yet all met:
- forward-chained, strictly prospective cuts
- the settlement moves only along a legal status edge and carries evidence
- a falsification blocks only the affected production path and rewrites no model output.

## Exact next action

Finish `A3`, then select `OAS1-FLAT-SURFACE`.

## Blockers

- **OAS1-TIEBREAK**: **owner decision, and it may not be taken by whoever has seen the scores.** The three variants' MAEs are now on the record (pass 1.159614 / 1.159721 / 1.159993 for A / none / B), so any rule written here is a selection rule written after seeing them — the exact thing the one-SE rule and the tie-break exist to prevent. The gap is pre-existing in the pre-registration, not introduced by amendment A1.
- **OAS1-HURDLE**: the captured 2026 play-by-play carries **week 1 only** — read from the file, 2,756 rows, zero at ordinal 202602. Scoring needs Week-2 plays, which are bytes outside this checkout. **Assigned, not blocked for both agents**: the request is in `docs/AGENT_OUTBOX.md`. The candidate's unit strengths are committed before those bytes arrive, which is the position a hurdle test should be run from.
- **A4**: the remaining estimand is not yet measurable. Completing it before it is measurable would manufacture a settlement, which is the failure mode the registry exists to prevent.
- **GAME_STATE**: prerequisite work has not made it lawful. The T1-C pregame-only generator exists; what is missing is the evidence that a game-state layer may consume it without a postgame path.
- **OUT-022C**: no FanDuel salary export is in the repository and none can be fetched from here. **This is assigned, not blocked for both agents**: it needs bytes from outside this checkout. FanDuel provenance stays `VERIFIED_RULE_VALUE_RELAYED_SOURCE` and is not upgraded.

## Commands to resume

```
cd /home/user/nfl
git log --oneline -3
python3.12 nfl/tools/agent_state.py --write   # refresh this file
python3.12 nfl/tests/run_suite.py             # full suite, ~45 min
python3.12 nfl/production/pipeline.py --audit-reads
```

Write files through `cat <<'EOF' | python3.12 nfl/tools/nflwrite.py <path>`; it refuses a write outside this repository. `python3.12` only. Stage selectively, never a broad `git add`.
