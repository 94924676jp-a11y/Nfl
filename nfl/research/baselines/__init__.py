"""D8 frozen simple baselines. DIAGNOSTICS ONLY.

Nothing in this package may be imported by `nfl/production/**` or
`nfl/product/**`. A baseline is a comparison object; the moment it is served it
stops being one, because the thing it exists to measure would then contain it.
"""
